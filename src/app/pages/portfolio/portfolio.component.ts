import { Component } from '@angular/core';

/** Groups the original portfolio sections so they can sit behind a route. */
@Component({
  selector: 'app-portfolio',
  standalone: false,
  templateUrl: './portfolio.component.html',
})
export class PortfolioComponent {}
